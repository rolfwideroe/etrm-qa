@model IEnumerable<PreTestWeb.Models.PreTest>

@{
    ViewBag.Title = "Pretest Index Page";
}

<h2>PreTests</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Active)
        </th>
        <th>
           Prod Server
        </th>
        <th>
            Test Server
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Active)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProdServer.ServerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TestServer.ServerName)
        </td>
        <td>
            @Html.ActionLink("Setup Workspace", "CreateWorkspace", new {id = item.PreTestId})  |
           @* @Html.ActionLink("Run", "Run", new {id = item.PreTestId})  |*@
            @Html.ActionLink("Workspaces", "NavigateToWorkspaces", new {id = item.PreTestId}) |
            @Html.ActionLink("Edit", "Edit", new { id = item.PreTestId }) 
            @*@Html.ActionLink("Delete", "Delete", new { id = item.PreTestId })*@
        </td>
    </tr>
}

</table>
